#include <bits/stdc++.h>
using namespace std;


int main()
{	ios_base::sync_with_stdio(false);// to avoid TLE
    cin.tie(NULL);
	long long n;
	cin>>n;
	vector<long int> p;
	long long num;
	long long sum=0;
	for(long i=0;i<n-1;i++)

     {

          cin>>num;
			p.push_back(num);
          sum=sum+push_back[i];

     }
	 long long sumAp=n*(2+(n-1)*2)/2;;
	 
	

	long long k=sumAp-sum;
	cout<<k;
	return 0;
}
